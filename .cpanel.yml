---
deployment:
  tasks:
    - export DEPLOYPATH=/home/marcusmilesco/public_html/admin.marcusmiles.co.ke
    - /bin/rsync -avz --exclude '.git' --exclude '.cpanel.yml' ./ $DEPLOYPATH
    
    # New Frontend Build Steps:
    - cd $DEPLOYPATH
    - /usr/local/bin/npm install
    - /usr/local/bin/npm run build